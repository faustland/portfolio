
$(document).ready(function(){
    var h = $(window).height();

    $("#imgStart1").eq(0).addClass('fade-in-up2');

    $(window).scroll(function(){
        if ( ($(this).scrollTop()+h) >= $("#img1").offset().top) {
            $("#logo-color").eq(0).addClass('logo-hidden-js');
        } else {
            $("#logo-color").eq(0).removeClass('logo-hidden-js');
        }
        if ( ($(this).scrollTop()+h) >= $("#img2").offset().top) {
            $("#img2").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img3").offset().top) {
            $("#img3").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img4").offset().top) {
            $("#img4").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img5").offset().top) {
            $("#img5").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img6").offset().top) {
            $("#img6").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img6_2").offset().top) {
            $("#img6_2").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img7").offset().top) {
            $("#img7").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img8").offset().top) {
            $("#img8").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img8_1").offset().top) {
            $("#img8_1").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img9").offset().top) {
            $("#img9").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img11").offset().top) {
            $("#img11").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img12").offset().top) {
            $("#img12").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img13").offset().top) {
            $("#img13").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img14").offset().top) {
            $("#img14").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img17").offset().top) {
            $("#img17").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img18").offset().top) {
            $("#img18").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img19").offset().top) {
            $("#img19").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img20").offset().top) {
            $("#img20").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img21").offset().top) {
            $("#img21").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img22").offset().top) {
            $("#img22").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img23").offset().top) {
            $("#img23").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img24").offset().top) {
            $("#img24").eq(0).addClass('fade-in-up2');
        }
        if ( ($(this).scrollTop()+h) >= $("#img-bottom-1").offset().top) {
            $("#img-bottom-1").eq(0).addClass('fade-in-up2');
        }


    });
});
